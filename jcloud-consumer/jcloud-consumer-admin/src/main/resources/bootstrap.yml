spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: jcloud-config
        enabled: true
      name: jcloud-consumer-admin
      profile: dev
      username: root
      password: root

eureka:
  client:
    service-url:
      defaultZone: http://root:root@localhost:8761/eureka/