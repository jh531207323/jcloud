
#加载-本地物理环境
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

#加载-SVN仓库
#spring:
#  profiles:
#    active: subversion
#  cloud:
#    config:
#      server:
#        svn:
#          uri: http://192.168.0.1/svn/repo/config-repo
#          username: username
#          password: password
#        default-label: trunk
